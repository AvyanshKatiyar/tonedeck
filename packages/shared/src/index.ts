/**
 * @tonedeck/shared — public surface for shared DSP types, schema, and utilities.
 * Placeholder module: later tasks (B onwards) will add preset.ts, biquad.ts, etc.
 */

export const VERSION = '0.1.0'

/**
 * Placeholder namespace re-export point.
 * Future sub-modules (preset schema, biquad math, YAML emitter) will be
 * exported from here so consumers import from '@tonedeck/shared' only.
 */
export type PlaceholderModule = Record<string, never>
